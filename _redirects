# Maintenance mode is OFF — site is live. To go back into maintenance,
# uncomment the rule below (the trailing "!" forces it to win over
# real files like index.html, which Netlify would otherwise shadow it
# with) and redeploy.
# /*    /maintenance.html   503!
