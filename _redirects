# Maintenance mode is OFF — the real site serves normally.
# To go back into maintenance, uncomment the rule below (the trailing
# "!" forces it to win even though matching files like index.html
# already exist in the deploy — Netlify normally lets real files
# shadow redirect rules).
# /*    /maintenance.html   503!
